==BEFORE USAGE==
-you should have ApacheAnt in your $PATH
-download groovy-all.jar from any maven repo to ${devtool-dir}/lib

To use devtool:

-navigate to checkout dir
-type ant -p

you will see available ant targets

